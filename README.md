# Cheapest Flight Python App

Find the "Cheapest Flight Withing K Stops" solutions using priority-queue algorithm.
It loads input from a JSON file and prints the result to the terminal.

## How to Run

```bash
python main.py flight_route_test.json
```

## How to Run Unittest

```bash
python -m unittest discover -s tests
```
